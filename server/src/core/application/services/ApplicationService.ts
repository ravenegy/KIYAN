/**
 * Base ApplicationService class for organizing cross-cutting application logic 
 * that doesn't fit cleanly into a specific Command or Query handler.
 */
export abstract class ApplicationService {
  protected constructor() {}
}
