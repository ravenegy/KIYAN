import { IContainer } from '../../../core/di';

export class ProductionConfigurationRegistration {
  public static register(container: IContainer): void {
    // Configuration registration is handled by the ProductionInfrastructureBootstrapper
  }
}
