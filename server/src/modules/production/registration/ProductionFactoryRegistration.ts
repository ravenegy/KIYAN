import { IContainer } from '../../../core/di';

export class ProductionFactoryRegistration {
  public static register(container: IContainer): void {
    // Factory registration is currently handled by the InfrastructureBootstrapper
  }
}
